# dotnet_sample — Starter API

This is a minimal ASP.NET Core Web API used as a **private review target** for the AI code-review demo. It is intentionally small — a few controllers, a service, and a model — so the review agent has something realistic to scan without needing a full application.
